package dtu.token;

//@Path("/tokens")
public class TokenResource {

    // GET token(accountId) - return one token associated to accountId

    // GET validate(token) - validates if token is valid

}